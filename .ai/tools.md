# Tools

- **Python:** `process_video.py` (and optional `process_all.py`) for Gemini analysis + FFmpeg frame extraction. Run from project root; require `requirements.txt` and secure API key.
- **Next.js:** `npm run dev` / `npm run build`. App Router under `src/app/`.
- **Editor agent:** Output to `/public/videos/` per `_notes/bjj-editor-prompt.md`.
- **Webmaster agent:** Consume technique metadata; write enrichment (diagrams, step refs) per schema defined in current-state / examples.
