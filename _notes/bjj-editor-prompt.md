#The "Assistant Editor Agent" Prompt Template

Filename & Export Format:

Export clips as .mp4 at 1080p.

Use "kebab-case" for filenames (e.g., triangle-choke-from-closed-guard.mp4).

Avoid special characters or spaces in the name.

Save the final file directly into the /public/videos directory of the web project.